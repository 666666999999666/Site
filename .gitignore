# dependencies
node_modules
.pnp
.pnp.js

# next.js
.next/
.next-*/
out/
build
dist
next-env.d.ts
*.tsbuildinfo

# testing
/coverage
/playwright-report/
/test-results/

# env
.env
.env.local
.env.*.local
!.env.example

# docker-compose 本地开发覆盖文件（不提交，生产部署用 docker-compose.yml）
docker-compose.override.yml

# db data
data/
.tmp-pgtest/

# local Markdown import sandbox (owner content stays private)
/drafts/*
!/drafts/.gitkeep

# generated Prisma Client (Prisma 7 generates into project source)
lib/generated/prisma/

# uploads (kept by volume in prod, git-ignored)
public/uploads/*
!public/uploads/.gitkeep

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# os
.DS_Store
Thumbs.db

# IDE
.idea
.vscode
*.swp

# AI tool configs
CLAUDE.md
AGENTS.md

# SSL certificates (private keys must never be committed)
nginx/certs/*.key
nginx/certs/*.crt
nginx/certs/*.pem

# superpowers scratch
.superpowers/
docs/superpowers/

# deploy artifacts
*.tar
*.tar.gz
*.zip
/backups/
/.deploy-state

# temp/debug files
giteecicd*.md

# local Git worktrees used to isolate parallel implementation branches
/.worktrees/

# Gitee Go pipeline config (tracked in git, contains no secrets)
# .workflow/pipeline-deploy.yml uses ${TCR_USERNAME} and ${TCR_PASSWORD} variables
