services:
  db:
    restart: "no"

  web:
    pull_policy: never
    restart: "no"

  nginx:
    restart: "no"
    ports: !override
      - "127.0.0.1:${QZ_HTTP_PORT:-18080}:80"
      - "127.0.0.1:${QZ_HTTPS_PORT:-18443}:443"
